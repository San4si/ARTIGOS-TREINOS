// if feito em uma linha só

let isMember = true;

let shipping = isMember ? 2 : 10;

console.log("Frete: " + shipping);

let age = 21;

let isAdult = (age >=18 ? 'sim' : 'não');

console.log(isAdult);

